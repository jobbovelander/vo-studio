services:
  vo-studio:
    build: .
    container_name: vo-studio
    restart: unless-stopped
    ports:
      - "5000:5000"
    volumes:
      - /volume1/vo_studio:/data
    environment:
      - VO_DATA_DIR=/data
